package fitness_app.exception_messages;

import fitness_app.annotations.Password;

public class PasswordExceptions {
    public static final String INVALID_PASSWORD = "Invalid password! Password must be with length between 10 and 30 symbols!";

    public PasswordExceptions() {

    }
}
